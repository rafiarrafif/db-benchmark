{
  "name": "os-benchmark",
  "type": "module",
  "private": true,
  "scripts": {
    "seed": "bun run ./seed.ts",
    "test": "bun run ./index.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.18.0",
    "prisma": "^6.18.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
